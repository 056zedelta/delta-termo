# jogo do termo versão terminal

To be done with front-end included

To run type ```make run```

Rules: 
  - type a 5 letter word.
  - if letter exists, it will be returned inside ().
  - if letter is on the right place, it will be return inside [].
